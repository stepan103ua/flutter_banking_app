import 'package:flutter/material.dart';

class AppBarThemeDecoration {
  static AppBarTheme get appBarTheme => const AppBarTheme(
        backgroundColor: Colors.transparent,
        elevation: 0,
        foregroundColor: Colors.black,
        centerTitle: true,
        toolbarHeight: 60,
      );
}
